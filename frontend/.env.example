# Comma-separated hostnames/patterns allowed through Vite's host check.
# Only needed when accessing the dev server via a reverse proxy.
# Example: VITE_ALLOWED_HOSTS=.elwin.cc,umple.example.com
# VITE_ALLOWED_HOSTS=
