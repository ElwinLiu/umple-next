// this example will not start tracing of
// attribute name until condition becomes true
// and then always continue tracing 

@SuppressWarnings("deprecation")
class Student
{
  name;
  trace name after [name == "john"];
}
